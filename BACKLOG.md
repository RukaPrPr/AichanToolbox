# 艾酱图片工具箱待处理清单

## 实施路线

按“先测量与启动基础设施，再改高频交互，随后调整响应式视觉，最后替换图片核心与发布结构”的依赖顺序执行：

1. **阶段 A｜启动链路与可观测性（已完成）**：启动计时、启动快照、配置单次水合、后台缓存清理、图片引擎延迟初始化。
2. **阶段 B｜画布渲染性能（已完成）**：统一视口矩阵、独立网格合成层、逐帧输入合并、端口坐标缓存、固定视口 WebGL 全量连接层与刷新率适配。
3. **阶段 C｜响应式与品牌视觉（已完成）**：`<1180px` 顶部下拉节点库、跨断点 FLIP 动画、圆形品牌 Logo 和各 DPI 布局。
4. **阶段 D｜JPG 执行语义与编码核心（主体完成）**：路径校验、`IJpegEncoder` 与 libvips→Jpegli 分块流式传输已完成；进程内 Jpegli 需引入官方动态库及稳定原生封装，暂不以不安全 P/Invoke 替代。
5. **阶段 E｜发布体积与兼容包（已完成）**：FFmpeg 可选组件、便携版/精简版双发行、Release 内容清理及完整回归测试。

每阶段先建立可复现基线，再实施和构建验证；阶段 D 的输出一致性通过后，才进入阶段 E，避免把编码回归误判为发布裁剪问题。

当前验证结果（2026-08-26，8.0.0）：前端与 WPF Release 构建无警告；图片/工作流/ZIP 引擎冒烟测试通过；JPG 保持 4:4:4；纯筛选未处理分支可合法直通并保持原文件，实际处理后的非 JPG 分支仍会被运行前校验拒绝；目标体积节点提供达标与未达标双出口，未达标结果可选择保留最小 JPG 或事务式回退到节点入口状态，二次缩放重试路径已通过冒烟测试；替换原图的回收站调用使用专用 STA 队列与现代 `IFileOperation`，并已在 G: 卷通过中文、日文及括号嵌套路径的实际回收验证；桌面宿主采用标准 WebView2，连接层采用固定视口 WebGL；节点在静止、选中、拖动与松开时始终复用同一 transform 渲染路径，节点拖动复用画布自定义光标，连接线跨越垂直位置时保持连续曲率。

.NET 10 迁移验证（2026-08-26）：主程序、引擎冒烟与目标体积探针均已切换至 `net10.0-windows`，使用 SDK 10.0.400 和 Runtime/Desktop Runtime 10.0.11 完成 0 警告构建；图片、工作流与 ZIP 冒烟测试全部通过；便携版确认内置 10.0.11，精简版确认不携带运行时，两个版本均可进入 WPF/WebView2 主窗口并以退出码 0 正常关闭；直接与传递 NuGet 依赖未发现已知漏洞。

启动性能复核（2026-08-27）：生产 `app.js` 经 Terser 从约 460KB 降至约 161KB，关闭未使用的 Vue Options API；启动遥测新增 HTML→模块、导航、资源、DOM、首帧和脚本就绪指标。启动窗先于主窗口显示，实时阴影改为静态径向光晕，Logo 呼吸动画限制为 30fps，退出淡出缩短至 110ms。预编译发布与普通版各交替实测三次，目录增大约 14.7%，`OnStartup` 中位数未改善，因此不纳入构建方案。

## JPG 统一输出链路优化

状态：主体完成；仅“进程内 Jpegli 原生封装”保留为后续独立升级项

已落地说明：单次 JPG 输出不再生成中间 PNG。libvips 以 32 行条带读取像素并通过受管道背压限制的 PGM/PPM 流直接送入 Jpegli；即使选择 16 路并行，也不会为每张大图分配完整 RGB 缓冲。目标体积节点为了复用同一底图进行多次真实编码，仍有意保留一份压缩的无损 PNG 工作底图。

- 最终输出统一为 JPG；保留 PNG、WebP、AVIF、HEIC 等格式的解码能力，不新增 PNG/WebP 输出编码器。
- 将完整 FFmpeg 从主程序拆为可选兼容组件；仅在 libvips 解码失败时调用，缺失时给出明确安装提示。
- 提供两种独立发行包：默认提供无需额外安装运行库的“便携版（self-contained）”，另提供体积更小的“精简版（framework-dependent）”；精简版启动时检测所需 .NET Desktop Runtime，缺失时显示明确版本、下载入口和离线安装说明，禁止无提示闪退。两种发行包使用同一套功能与配置格式，用户数据可以直接互通。
- 重构“最终统一输出 JPG”的工作流语义：导入时继续保留原始格式，格式筛选与大小筛选始终判断所在节点入口的真实当前结果；经过“转为 JPG”“JPG 画质压缩”或目标体积节点的达标/保留最小结果出口后，后续节点才按实际生成的 JPEG 数据与体积判断。目标体积未达标且关闭保留开关时，分支严格恢复节点入口格式与处理状态。运行前静态检查所有能够到达“保存输出”的分支：纯筛选且未经过任何图片处理的分支允许直通，由保存节点标记为“不处理”并保持原文件；只要分支经过缩放、逆网点化等实际处理，仍必须先生成 JPG。禁止在保存节点处静默补转 JPG，也禁止把导入文件提前伪装成 JPG。
- 建立 `IJpegEncoder` 抽象，将逐文件启动的 `cjpegli.exe` 逐步替换为进程内 Jpegli 原生库。
- 实现 libvips 到 Jpegli 的内存或扫描行传输，消除临时 PNG 的编码、写盘和回读。
- 为直接像素传输设置有上限的缓冲池，并与 1—16 路文件并行联动，避免大图批处理时内存失控。
- 保持从同一原始工作底图生成最终 JPG，禁止代际二次压缩。
- 保留目标体积节点对同一工作底图的多画质尝试，以及精确预估结果缓存复用。
- 清理 Release 发布内容：排除 PDB、前端 source map、开发配置、测试程序、源码资源和未参与运行的原始高清素材；仅发布实际使用且验证过的语言资源，同时完整保留第三方许可、用户说明和运行所需组件。
- 发布配置不将“单文件封装”当作真实减小安装体积的手段；任何裁剪或压缩发布选项都必须同时验证 WPF、WebView2、反射、原生 DLL 加载和启动耗时。
- 实施前后分别验证输出画质、4:4:4/灰度行为、目标体积结果、特殊格式兼容性、峰值内存、批处理速度与发布体积。

## 工作流画布拖动性能与高刷新率适配

状态：已完成；待在不同刷新率显示器上补充长期帧时间采样

已落地说明：画布统一使用单一 `translate3d + scale`，网格独立为合成层，指针事件按 `requestAnimationFrame` 合并。7.6.0 将 WPF 合成捕获宿主替换为标准 WebView2，并以固定视口 WebGL 图层取代大尺寸高 DPI Canvas；每个交互帧完整绘制全部可见连接线，节点毛玻璃和连线光晕在静止、拖动、缩放与平移时保持一致。连接线控制距离只依赖水平绝对距离与连续的垂直辅助量，不再在水平差值过零时从接近 0 突变到固定 72px。

8.0.0 将节点持久坐标和拖动偏移合并到挂载期恒定的 `translate3d` 表达式中，拖动只更新 CSS 变量；点击阈值以内不进入拖动态，松开时原子提交坐标且不清空 transform，同时移除拖动期间动态添加的 `will-change`，避免 Chromium/WebView2 在交互边界提升或撤销文字所在合成层。

- 建立拖动性能基线，统计平均 FPS、P95 帧耗时和超过单帧预算的长帧；覆盖 12 节点/19 连线、30 节点/60 连线，以及 30%、50%、100%、200% 画布比例。
- 重构视口变换：彻底停止使用 CSS `zoom/left/top` 移动画布，设置 `transform-origin: 0 0`，统一通过单一 `translate3d(x, y, 0) scale(zoom)` 驱动节点与连接线视口；同步改造屏幕坐标与世界坐标的正反变换。
- 将三层渐变网格从 `.workflow-surface` 背景中拆为独立、可合成的 `grid-layer`；平移时只变换图层，不修改三组 `background-position/background-size`，缩放档位变化时才更新或切换缓存网格纹理。
- 将节点拖动改为统一 `requestAnimationFrame` 调度；`pointermove` 只记录最新坐标，每个显示帧最多更新一次节点变换和活动连接线。
- 在拖动开始时缓存全部端口的世界坐标；拖动期间通过初始坐标加位移计算当前节点端点，不再逐帧执行全部端口的 DOM 查询和布局读取。
- 将连接线移入与画布视口等大的 WebGL 透明层；CPU 只生成贝塞尔曲线的小型三角带，GPU 负责彩色主线、白色高光、圆角端帽和两层柔光混合。
- WebGL 后备缓冲只随窗口尺寸变化，不随世界坐标边界、节点位置或缩放范围扩张；拖动、缩放和平移时不会重新分配覆盖整张工作流的高 DPI Canvas。
- 所有交互帧按路径高亮顺序完整重绘全部连接线，不采用只更新相关连线的视觉降级；节点实时毛玻璃、连接线光晕与背景采样始终开启。
- DOM 节点变换与 WebGL 绘制在同一个 `requestAnimationFrame` 中提交，避免连线落后一帧；松开后只提交持久化坐标，不切换另一套视觉状态。
- 帧率默认采用“自动”模式，通过 `requestAnimationFrame` 跟随窗口所在显示器的实际刷新节奏，不写死 60 FPS，也不使用无限循环。
- 可选增加 60 FPS 与 120 FPS 节能上限；自动模式覆盖 60/120/144/165/240Hz，并在窗口跨显示器后自动适配新的呈现节奏。
- 性能目标：60Hz 下常规工作流稳定接近 60 FPS、P95 帧耗时低于 16.7ms；120Hz 下力争 90—120 FPS。更高刷新率不承诺跑满，以不掉帧、不撕裂和不持续空转为准。
- 单独验证 `transform: scale()` 在 Windows 100%/125%/150%/200% 显示缩放及画布 30%—200% 下的文字、输入框、端口圆点和连接线清晰度；若静止后仍产生可见文字模糊，不得直接替换现有缩放方案。
- 验证节点经过无关连接线、路径高亮、多选置顶、缩放和平移时的层级、遮挡、圆角、光晕和静止外观均保持正确。

## 主界面左上角品牌 Logo

状态：已完成

- 程序完成启动后，左上角品牌 Logo 保持原 Logo 的圆形外观，不再使用圆角方形或渐变彩色外框。
- Logo 图片本身按正方形等比缩放并使用圆形裁切，禁止拉伸；外侧不添加彩色底框、彩色描边或彩色光环。
- 保持现有标题、英文副标题和版本号的排版位置不变，并验证 Windows 100%—200% 显示缩放下圆边抗锯齿清晰。

## 小尺寸顶部下拉节点库

状态：已完成

- 在窗口宽度 `<1180px` 时，彻底替换当前画布左侧的独立方形节点库按钮；900—1179px 与 `<900px` 使用同一套顶部下拉抽屉设计。
- 将横向毛玻璃节点库按钮放在顶部栏第二行左侧、主界面 Logo 正下方的预留区域，左边缘与 Logo 对齐；高度约 38px，宽度使用 `clamp(210px, 27vw, 258px)`，避免与右侧并行、黑白优化、缓存预估、精确预估和运行按钮重叠。
- 收起按钮包含节点库图标、“节点库”文字和右侧下拉箭头；打开时箭头旋转 180°并轻微增强边框与阴影，不再显示旧的孤立方形按钮。
- 抽屉从按钮正下方向下展开，左边缘和宽度与按钮一致，与按钮间隔约 7px；覆盖工作流画布而不改变画布布局，最大高度限制在顶部栏与底部状态栏之间，节点项目区域内部独立滚动。
- 继续复用同一份节点库 DOM，禁止为桌面侧栏和下拉抽屉维护两份节点列表，以保留滚动位置、焦点和控件状态。
- 打开和关闭使用约 220—240ms 的 `clip-path + opacity + translateY` 毛玻璃下拉动画，内容延迟约 50ms 淡入；不直接动画列表实际高度，避免重复布局和边框抖动。
- 展开时在画布区域显示很淡的毛玻璃遮罩；点击遮罩、按 `Esc`、点击关闭键或成功添加节点后收起抽屉。
- 跨越 1180px 时必须执行完整 FLIP 形变动画：桌面完整侧栏先淡出内容，毛玻璃外壳收缩并移动到顶部第二行按钮位置，圆角同步过渡，抵达后再显示按钮图标、文字和箭头；反向跨越时按相反过程展开为完整侧栏。
- 跨断点动画总时长约 270ms，并与顶部主/次功能组的位置 FLIP 动画同步；工作流画布同时平滑扩展或收缩，保持当前世界坐标中心、缩放比例和节点屏幕位置稳定。
- 从 `<1180px` 返回桌面层级时，如果抽屉已经打开，以当前抽屉矩形作为展开起点；如果抽屉关闭，则以按钮矩形作为起点。
- 快速反复跨越 1180px 时，取消旧动画后必须从幽灵外壳的当前视觉矩形继续正向或反向形变，禁止跳回动画起点；动画期间临时禁止节点库交互，结束后恢复。
- 软件启动时若窗口本身小于 1180px，直接呈现最终下拉按钮布局，不播放错误的桌面侧栏收缩动画；`prefers-reduced-motion` 下改用短淡入淡出。
- 验证 1179px/1180px、快速往返缩放、抽屉开/关两种跨断点状态、Windows 100%—200% 显示缩放及最低窗口尺寸，确保无瞬移、重影、重复抽屉、按钮碰撞、画布中心漂移或文字模糊。

## 启动性能优化

状态：已完成

- 将图片引擎改为线程安全的延迟初始化：程序启动和普通前端导航阶段不初始化 libvips，也不检查或启动 Jpegli；首次导入需要真实解码的特殊格式、读取普通元数据失败，或首次执行精确预估/运行工作流时才加载相应组件。
- 普通 PNG/JPG/WebP 的文件列表初始信息优先使用轻量元数据读取器，避免仅为显示格式、分辨率和大小而提前初始化 libvips；延迟初始化失败时返回明确错误且允许后续重试。
- 将历史 Cache 会话目录的扫描与删除移出 `DesktopBridge`/主窗口同步构造路径，在当前会话目录创建完成后以低优先级后台任务执行；严格排除当前进程会话目录，清理失败只记录日志，不阻塞 WebView2 导航或程序退出。
- 将 `app.ready`、`profiles.list` 和启动时的 `profiles.load` 合并为一次启动快照请求，一次返回版本、窗口状态、处理器数量、文件/ZIP 列表、配置名称、记忆中的当前配置及其工作流内容。
- Vue 启动时先取得完整启动快照，再对 Pinia 执行一次批量水合并挂载/显示主界面；禁止先渲染默认工作流、随后加载收藏工作流造成节点、连接线和 Canvas 的重复创建与重绘。
- 启动层 Logo 使用专门生成的 256×256px 清晰资源，并按实际显示尺寸解码；原始高清 Logo 继续保留在源码资源目录中，不随启动路径进行全分辨率解码，也不得覆盖原始素材。
- 增加基于单调时钟的内部启动计时点，至少分别记录：进程/WPF 初始化、窗口首次显示、WebView2 环境创建、WebView2 导航完成、Vue 脚本启动与挂载、配置快照读取、libvips 首次初始化、Jpegli 首次调用及前端 `app.frontendReady`。
- 启动计时日志默认保持轻量，不在 UI 线程同步写入大量内容；开发构建可输出详细阶段，发布构建只保留汇总与异常阶段，并能区分冷启动、热启动和首次图片引擎加载。
- 验证无配置、有默认收藏配置、大量收藏配置、残留大缓存目录、缺少 WebView2、缺少/损坏图片组件及首次导入特殊格式等场景，确保启动界面不会黑屏、默认工作流不会短暂闪现、后台清理不会误删当前缓存。

## 画布拖动自定义光标

状态：已完成

- 采用本轮生成的方案一：白色填充、深灰圆角轮廓和少量淡紫色点缀的极简手形光标。
- 分别制作“可拖动”的张手状态与“正在拖动”的握手状态，统一应用于画布空白区域和节点标题栏；拖动激活后使用全局握手状态，端口、按钮和其他控件在非拖动状态下继续使用各自光标。
- 将选定概念重新绘制为透明 SVG 矢量资源，不直接缩放概念预览图；由 Chromium 按当前 DPI 光栅化，避免维护多套位图时产生错配。
- 两种状态均以 32px 逻辑尺寸和掌心 `(16,16)` 为热点，Windows 高 DPI 下保持位置一致，不出现锯齿、跳位或状态切换偏移。
- 验证画布悬停、按下拖动、松开、离开窗口以及节点和空白区域之间切换时，光标状态正确且无闪烁。
