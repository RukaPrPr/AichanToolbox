# 更新日志

本文件记录艾酱图片工具箱面向用户的重要变更。

## [8.1.0] - 2026-08-28

### 主题与外观

- 新增“石墨紫”深色主题：石墨灰背景与面板、低饱和紫色操作控件；原有银白外观保留为默认浅色主题，顶部菜单可切换。
- 主题名称、明暗属性和语义配色集中注册，菜单自动生成，控件共用主题样式；新增主题无需修改桌面端主题分支，扩展方式见 `frontend/THEMES.md`。
- 保存主题选择，并同步启动窗、原生主窗口、WebView2 与网页底色；损坏或不可用的偏好安全回退。主题不写入工作流配置。
- 补充早期恢复、存储失败、浅色还原和未来主题注册的回归测试；为新增主题菜单预留紧凑工具栏空间。
- 节点端口最外圈跟随中心色，修正深色主题下变黑的问题；普通、悬停、吸附和连接提示动画共用此规则，后续新增主题同样适用。
- 新增“黑金女仆”主题：炭黑面板、暖金控件、象牙白文字和可选金色边角，使用用户确认的艾酱重绘立绘。
- 立绘固定在可见画布左下，不随节点平移或缩放；平移、节点拖动/调整大小、连线和缩放时从 45% 淡至 20%，停止输入后恢复，不拦截操作，支持减少动态效果。
- 各主题定义拆分为独立文件；共享立绘组件支持素材、左右锚点、尺寸及透明度配置，装饰按主题可选。生产构建自动打包主题资源，增加二次元主题无需复制控件或改原生窗口逻辑。
- 新增装饰清理、滚轮合并、按住交互、快速重入和计时器释放回归测试，并补充主题添加示例与素材检查清单。
- 修复节点库底角装饰跟随列表滚动的问题：固定面板外框与内部滚动区分离，普通侧栏与窄窗口抽屉共用，后续主题同样生效。
- 补齐节点库上方两枚角标，四角共用独立固定装饰层；删除程序外框四枚角标，主题扩展收敛为仅供节点库使用的 `libraryOrnament` 配置。
- 窄窗口节点库抽屉与顶部功能区、左侧窗口边缘、底部状态栏统一保留 14px 间距，避免贴住上方功能区。

### 窗口交互入口

- 网页发起窗口移动、缩放时，先返回 WebView2 消息回调，再由 UI Dispatcher 启动系统交互循环，避免在回调内嵌套原生移动/缩放循环；窗口关闭后不执行排队请求。
- 补充入口调度和关闭保护回归测试。这项入口修正不改底栏、原生布局或渲染策略，不代表底栏滞后及边框裂缝已解决。

### 验证与范围

- 16 项前端回归、Vue/TypeScript 类型检查、WPF Release 构建、启动与主题持久化/背景同步冒烟、图片/工作流/ZIP 引擎冒烟及目标体积探针构建通过。
- 普通侧栏与窄窗口抽屉已验证四角固定、滚轮穿透、抽屉开合、三主题切换和跨断点往返；抽屉上、左、下实测间距均为14px。
- 沿用现有 .NET 10、图片引擎及 Portable/Slim 双发行结构，不纳入底部状态栏绘制或原生窗口布局实验。

## [8.0.2] - 2026-08-27

### 启动交接黑屏修复

- 修复 Logo 呼吸结束、进入主界面时整个程序瞬间闪黑的问题；启动专用测试版已由用户确认问题解决。
- 仅临时启动窗启用逐像素透明，退出动画改为淡出内容层，不再动画整个原生窗口的透明度；保留 Logo 呼吸和 110ms 淡出，主窗口继续使用不透明的标准 WPF/WebView2。
- 原生主窗口清除色、WebView2 默认背景与网页浅色背景保持一致；Rollup 复用开发入口模板，防止生产包重新引入深色启动底面。
- 前端等待字体、图片解码和两次真实动画帧后再请求揭示主界面，移除将 80ms 定时器视作绘制完成的回退；宿主在淡出结束后才报告交接完成，并在关闭时取消未完成操作。

### 验证与范围

- 新增 7 项前端启动回归及 WPF 启动属性/动画冒烟；图片、工作流和 ZIP 引擎冒烟继续通过。
- 沿用 .NET 10 和现有运行时依赖；构建工具 Terser 固定为已验证的 5.50.0，保留依赖安全检查。
- 本版不包含窗口缩放、原生底部状态栏或边框撕裂相关实验。这些问题仍待处理，已按用户要求暂停排查。

## [8.0.1] - 2026-08-27

### Slim ZIP 封装修订

- 正式 Slim ZIP 改用已验证的 RepackTest1 封装，仍使用原文件名及 8.0.1 版本号；不重新编译、不移动版本标签，Portable 包保持不变。
- 包内 33 个文件的路径、长度和逐文件 SHA-256 与原正式包完全一致；只改变压缩数据、条目顺序及 ZIP 元数据，未包含诊断性依赖升级或 Diagnostic1 构建。
- ZIP 从 13,693,761 字节降至 13,006,975 字节；31 个文件使用 Deflate，2 个 PNG 使用 Store。
- Slim ZIP 新 SHA-256 为 `5a016f569ea5cc45f049175e251cf8478beeabcdfdbb8f79875b3161571da700`；此前同名 ZIP 的 SHA-256 为 `5474fb51389297258eae5f1a7bdcd27a39c62336531c71cc74ec0216643dbca3`，发行附件中的 `SHA256SUMS.txt` 同步更新。
- 本机对照中，RepackTest1 经真实浏览器下载并保留互联网来源标记后，指定 Microsoft Defender 扫描未发现威胁，未复现原 ZIP 的下载拦截；正式 Slim 附件替换后也已重新下载、核对校验值并复扫，未发现威胁。上述结果不是 Microsoft 的误报认定或跨设备安全保证。

### 启动与性能

- 启动窗改为先于主窗口和 WebView2 初始化显示，降低冷启动阶段的黑屏感。
- 移除启动窗实时阴影，改用静态径向光晕；Logo 呼吸动画限制为 30 FPS，退出淡出由 170ms 缩短至 110ms。
- 生产前端启用 Terser 两轮压缩并关闭未使用的 Vue Options API，`app.js` 从约 460 KB 降至约 161 KB。
- 扩展启动遥测，分别记录 HTML、脚本与样式资源、DOM、首帧、Vue 挂载、启动快照和 WebView2 阶段。
- 对 ReadyToRun 进行了交替冷启动测量；因发布目录增大约 14.7% 且启动中位数没有改善，本版本维持普通发布方式。

### 界面与构建

- 画布与节点标题栏改用艾酱主题 `28×28px` 透明拖拽光标，移除模糊星星装饰，并重新处理透明边缘与箭头热点。
- 前端构建会先安全清理桌面端 `wwwroot` 输出目录，避免旧光标和过期静态资源残留。
- 发布包继续提供 Portable 与 Slim 两种 Windows x64 版本；默认不包含可选 FFmpeg 兼容组件。

## [8.0.0] - 2026-08-26

- 桌面端迁移到 .NET 10，便携版内置 .NET Desktop Runtime 10.0.11。
- 目标体积压缩增加“达标”和“未达标”双出口，以及未达标回退或保留最小候选机制。
- 节点与 WebGL 连接线统一坐标和合成路径，改善缩放、拖动稳定性与连接线曲率。
- 完善 JPG 输出语义、ZIP 工作流安全、替换文件回收站处理和引擎冒烟测试。

[8.0.2]: https://github.com/RukaPrPr/AichanToolbox/compare/v8.0.1...v8.0.2
[8.0.1]: https://github.com/RukaPrPr/AichanToolbox/compare/v8.0.0...v8.0.1
[8.0.0]: https://github.com/RukaPrPr/AichanToolbox/releases/tag/v8.0.0
